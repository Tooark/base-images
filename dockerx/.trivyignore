# Justificativa:
# Exceções temporárias de CVE aceitas para esta imagem em contexto de pipeline.
# Usar somente para CVEs especificas com correção pendente/upstream.
#
# Revisão:
# 2026-09-01 (ou antes, ao atualizar base/pacotes da imagem).

# usr/local/libexec/docker/cli-plugins/docker-buildx (gobinary) - github.com/docker/docker
CVE-2026-34040
